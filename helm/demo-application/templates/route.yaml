apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: {{ include "demo-application.fullname" . }}
  labels:
    {{- include "demo-application.labels" . | nindent 4 }}
spec:
  host: demo-appication.apps.cluster-8d6f5.8d6f5.sandbox1821.opentlc.com
  port:
    targetPort: {{ .Values.service.port }}
  to:
    kind: Service
    name: {{ include "demo-application.fullname" . }}
    weight: 100
  wildcardPolicy: None
